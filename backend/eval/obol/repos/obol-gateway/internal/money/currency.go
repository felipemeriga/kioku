package money

// Common currency codes used across the Obol sample data. These are provided as
// named constants so call sites don't sprinkle string literals; any valid
// ISO-4217 code is still accepted by the constructors.
const (
	EUR = "EUR"
	USD = "USD"
	GBP = "GBP"
)
