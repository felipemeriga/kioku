# Local-dev env for frontend.
# Copy to frontend/.env.local and fill in values from `supabase status`.
# Vite auto-loads .env.local when running `npm run dev`.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<paste ANON_KEY from `supabase status --output env`>
